{
  "name": "@nexus/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "build": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@nexus/contracts": "*",
    "@nexus/domain": "*",
    "@nexus/ui": "*",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  }
}
