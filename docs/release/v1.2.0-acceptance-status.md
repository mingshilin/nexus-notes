# v1.2.0 Candidate Acceptance Status

This is a candidate, not a published or fully accepted release. Existing
v1.1.0 and v1.1.0-ai-assistant-fluency releases must remain unchanged.

## 2026-09-12 Browser Check

The current Edge extension connected and exposed an authenticated Preview
workspace. No credentials or browser session stores were read. The page showed
an unsaved-change indicator, so no existing note content was edited or saved.

Interaction remains BLOCKED: Page.captureScreenshot timed out and the native
Expand action for the note inspector exceeded its deadline before command
dispatch. This is insufficient evidence to blame application behavior, and is
not a successful UI, visual, or action-execution acceptance result.

## Test Harness Correction

The main authenticated smoke now checks the application auth boundary before
attempting note creation, reusing the standalone scenario's preflight. An
expired profile produces AUTHENTICATED_PROFILE_REQUIRED, BLOCKED and exit 2
instead of misleading new-note selector timeouts. Actual application failures
still fail; this change does not waive authentication or seed production data.

Two added preflight tests failed before implementation and passed afterward.
The full release-smoke file passed 23 tests. Lint and build passed; entry JS
remains 382.43 kB with no Vite >500 kB warning.

## Subsequent Evidence

- CI run `34681333202` passed for the smoke preflight correction (`6dbbd53`).
- Local Miniflare D1 restoration passed twice: 27 migrations, 227 statements,
  zero foreign-key violations; 9 users and 44 notes agree with the SQLite check.
- Five repository-external backup files passed DPAPI encrypt/decrypt comparison.
  This is Windows-user-bound encryption, not portable disaster recovery.
- See `2026-09-12-restore-verification.md` for exact scope and limitations.
- Root package and npm lockfile versions are aligned to candidate `1.2.0`.
  No tag or published release is implied by this metadata.

## Open Gates

- Fresh CI for the final candidate commit (the previous green run is not enough).
- Latest-source Preview deployment and authenticated note, database, AI,
  navigation, mobile and recovery acceptance.
- Exposed provider credential rotation.
- Fresh production backup and portable/off-machine recovery readiness before
  deployment; local D1 and DPAPI results do not prove remote restoration.
- Final release metadata, PR merge, immutable tag and published release.

No deployment, migration, release tag or PR merge was performed in this check.
